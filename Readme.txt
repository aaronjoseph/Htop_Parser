Code to view the CPU utilisation over time

Here, the steps to run the code are

1. pip(3) install -r requirements.txt
2. Run the command streamlit run Plotter.py which will run the streamlit application over web
3. You can alway modify the data and rerun on the streamlit

Or
1. Run sh.Quickstart.sh
